# 体感健身游戏 · Motion Fitness Game

[中文](README.md) · [English](README.en.md)

用普通摄像头做动作识别的健身小游戏：**22 个动作**分成 **上肢 / 下肢 / 核心 / 全身 / 拉伸** 五大类，
主页按分类挑选动作，进去就能练；**计数类自动数次数、计时类自动计时**，
并且**按「动作要领」一步一步计分**——每达成一个要领就立刻加分、响铃、用语音念出要领。

界面支持 **中文 / English** 两种语言，点右上角的 **⚙️ 设置**随时切换（语言、识别模型、音效、背景音乐都在里面）。

纯前端实现，MediaPipe 姿态模型与 wasm 全部放在本地 `vendor/` 目录，**不联网、不上传画面**。

---

## 目录

- [功能亮点](#功能亮点)
- [环境要求](#环境要求)
- [安装](#安装)
  - [第 0 步：拿到代码](#第-0-步拿到代码)
  - [第 1 步：安装 Node.js](#第-1-步安装-nodejs)
  - [Windows 安装 Node.js](#windows-安装-nodejs)
  - [macOS 安装 Node.js](#macos-安装-nodejs)
  - [Linux 安装 Node.js](#linux-安装-nodejs)
  - [不想装 Node.js？用 Python 也行](#不想装-nodejs用-python-也行)
- [启动与访问](#启动与访问)
- [摄像头权限设置](#摄像头权限设置)
- [动作主页与动作页](#动作主页与动作页)
- [🖐 一组结束后的手势圆环（退出 / 再做一次）](#-一组结束后的手势圆环退出--再做一次)
- [设置弹窗（语言 / 模型 / 声音）](#设置弹窗语言--模型--声音)
- [运动前校准](#运动前校准)
- [怎么用（机位很重要）](#怎么用机位很重要)
- [动作库总览（22 个动作）](#动作库总览22-个动作)
- [计分规则](#计分规则)
- [多语言](#多语言)
- [站不进轮廓 / 没反应？排查四步](#站不进轮廓--没反应排查四步)
- [常见问题](#常见问题)
- [测试](#测试)
- [目录结构](#目录结构)
- [想加动作 / 调分值 / 改阈值](#想加动作--调分值--改阈值)
- [隐私与许可](#隐私与许可)

---

## 功能亮点

- **动作主页（五大分类）**：进站先看到分类动作墙 —— 上肢（3）· 下肢（6）· 核心（6）· 全身（5）· 拉伸（2），
  每个动作一张卡片（图标 + 名称 + 计数/计时 + 目标 + 判定依据），还有搜索框。点卡片进动作页，左上角的 🏠 图标随时回来。
  「开合跳」在全身分类里，默认目标 50 次。
- **主页只管挑动作（摄像头也会关掉）**：从动作页返回主页时**自动关闭摄像头**（指示灯灭、省电），
  再点任意动作会自动重新打开，不需要多点一次。
- **动作页 + 设置弹窗**：动作页右上角是 ⚙️ 设置，弹窗里集中放**语言、识别模型、音效、语音报数、背景音乐**，
  以及镜像、火柴人、角度、识别指标等开关 —— 不用再满屏找按钮。
- **运动前校准**：开始前画面里有一个**虚线人体剪影**（只勾勒外形，不用对齐骨骼），画面上方同步用文字提示
  「进入虚线轮廓内」；**只要识别到人体、并且全身都在画面里就放行**（约 0.6 秒），
  距离、左右、高度、机位、站定都只是建议（面板里用「·」标出），不再拦着开始。
- **按动作要领逐步计分**：每个动作拆成 4~5 个可判定的步骤，做对一步立刻加分、响铃、打勾；
  整轮要领全部完成有满分奖励；计时类动作**每保持 1 秒 +1 分**。
- **关键帧就是唯一判据**：每个动作在进度条上拆成 4~5 个关键帧，**做到一格就点亮一格、加一格的分**；
  **全部关键帧都做到 = 计一次**。没有严格模式，也不再有单独的「计次判据」——判据与分数都挂在关键帧上
  （`tests/test-specs.mjs` 逐动作核对「链走完」与「计次」的同帧关系）。
- **有效计数判定（只有宽松一档）**：**大体上做到了就算一次** —— 浅一点的自重深蹲/箭步蹲、只放一半多的俯卧撑、
  顶得不太高的臀桥都会计数，同时用语音把「蹲低一点 / 再放低一点 / 屁股再顶高一点 / 别塌腰」纠正出来，
  分数也会按质量打折。完全没做到位的（只是晃了一下）不会计数，也不会碎碎念。
- **每个动作都写着「判定依据」**：卡片与动作页会标明这个动作是靠什么判定的（肘关节屈伸 / 膝关节屈伸 /
  髋部抬起高度 / 双脚离地 / 身体姿势 …），摄像头判不准的动作会额外标注**粗略判定**。
- **实时状态反馈**：画面下方永远显示「现在是什么状态、卡在哪一步、还差多少度」。
- **🐞 识别指标面板（含「计数诊断」）**：除了原始数字（视角、可见度、各关节角度），第一行就是**计数诊断**——
  现在处在哪个阶段、这一轮的结算线（回到哪里才算一次）、本轮最小值/峰值、以及**上一次为什么没计上**
  （太浅 / 太快 / 幅度太小 …）。计数不对劲时把这一行念出来就能定位，不用猜。
- **按当前语言的语音报数 + 音效**：要领达成有音阶上行提示音，第一次达成会念出要领，每 50 分报一次分数。
- **以语音激励为主，指导适度**：**每做一个动作都报数**（不会被别的提示吞掉），每 3 次给一句**激励**
  （加油 / 太棒了 / 继续坚持 / 做得很好 / 保持住 / 优秀 …，轮换不重复），达标时先念达成再补一句鼓励；
  姿势纠正保留但明显降频（同一句 15~30 秒不重复，做过几次之后进一步放慢），「下一步做什么」也放慢到 9 秒一句
  —— 话语权留给报数与鼓励，基本不用盯着屏幕。
- **🎶 四首背景音乐可选（只在动作页播放）**：全部现场合成（不占空间也不需要联网），设置里「🎵 背景音乐曲目」随便挑 ——
  **回到主页会自动停掉背景音乐**（主页只是挑动作，不需要音乐），再进动作页自动接着放，你的选择会被记住。
  **城市跑动**（132 BPM，轻快摇摆）/ **霓虹脉冲**（144 BPM，四踩电子感）/ **晨光放克**（122 BPM，放克切分）/ **动力驱动**（152 BPM，摇滚推进）；
  鼓组是真合成的（底鼓、军鼓、踩镲、拍手），节奏感比原来强得多；念要领时会自动把音乐压低，不抢语音。
- **🦴 火柴人开关**：可隐藏骨架只留摄像头画面；**角度标注**可独立开关 ——
  打开后画面上会标出**这个动作真正被判的那个角度**（深蹲标膝/髋、俯卧撑标肘、**仰卧抬腿标「髋」**（腰腿夹角）+ 膝角），
  一边做一边能看到「离判决线还有多远」；判据不是关节角的动作（开合跳、波比跳、跳跃类）不标，免得画面全是数字。
- **目标进度环、最佳成绩与训练记录**（保存在浏览器本地）。
- **离线可用**：模型与 wasm 都在本地，断网也能跑；画面不上传。

---

## 环境要求

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows 10/11、macOS 11+、任意主流 Linux 桌面发行版 |
| 浏览器 | Chrome / Edge 91+、Safari 16.4+、Firefox 89+（需要支持 WebAssembly SIMD） |
| 摄像头 | 笔记本内置摄像头或 USB 摄像头均可，建议 720p 及以上 |
| Node.js | **可选**。用自带的 `preview-server.js` 需要 Node 18+；不想装 Node 也可以用 Python |
| 网络 | **只在获取代码时需要**；运行与识别全程离线 |

---

## 安装

### 第 0 步：拿到代码

**方式 A：下载 ZIP（推荐给不想折腾 git 的人）**

1. 打开 <https://github.com/playteka/motion-fitness-game>
2. 点绿色的 **Code** 按钮 → **Download ZIP**
3. 解压到任意目录，例如 `C:\Users\你的用户名\motion-fitness-game`（Windows）或 `~/motion-fitness-game`（macOS / Linux）

**方式 B：用 git 克隆**

```bash
git clone https://github.com/playteka/motion-fitness-game.git
cd motion-fitness-game
```

> 仓库里已经包含了 MediaPipe 的 wasm 与姿态模型（约 33MB），所以**不需要**再执行任何安装依赖的步骤，没有 `npm install`。

### 第 1 步：安装 Node.js

只需要 Node 来跑自带的本地静态服务器（十几行代码）。**已经装过就跳过**，用 `node -v` 检查：

```bash
node -v      # 需要 v18 或更高，v20/v22 都可以
```

---

### Windows 安装 Node.js

**方式 1：winget（Windows 10 1809+ / Windows 11 自带，最省事）**

打开 **PowerShell**（开始菜单搜索 "PowerShell"），执行：

```powershell
winget install OpenJS.NodeJS.LTS
```

**方式 2：官方安装包**

1. 打开 <https://nodejs.org/> → 下载 **LTS** 版的 **Windows Installer (.msi)**（64-bit）
2. 双击安装，一路 Next；**务必保持默认勾选的 "Add to PATH"**
3. 安装完成后**关掉再重新打开** PowerShell / 命令提示符

**验证：**

```powershell
node -v
npm -v
```

两条都能打印出版本号（例如 `v22.14.0` / `10.9.2`）就成功了。

> 如果提示「无法将"node"项识别为 cmdlet」，说明 PATH 没生效：关掉终端重开；还不行就重新安装并确认勾选了 Add to PATH。

---

### macOS 安装 Node.js

**方式 1：Homebrew（推荐）**

如果还没装 Homebrew，先在「终端」里执行官网给出的那条命令（<https://brew.sh>）：

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

然后：

```bash
brew install node
```

**方式 2：官方安装包**

1. 打开 <https://nodejs.org/> → 下载 **LTS** 的 **macOS Installer (.pkg)**
2. 双击、一路继续安装
3. 打开「终端」（Terminal）验证

**验证：**

```bash
node -v
npm -v
```

> Apple 芯片（M 系列）和 Intel 芯片都可以用官网的通用安装包；Homebrew 会自动装对应架构的版本。

---

### Linux 安装 Node.js

**Debian / Ubuntu / Linux Mint**

系统仓库里的版本往往偏旧，推荐用 NodeSource 的 LTS 源：

```bash
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt install -y nodejs
```

**Fedora / RHEL / CentOS**

```bash
sudo dnf install -y nodejs
```

**Arch / Manjaro**

```bash
sudo pacman -S nodejs npm
```

**openSUSE**

```bash
sudo zypper install nodejs20
```

**通用做法：nvm（任何发行版都能用，且不需要 sudo）**

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
# 重新打开终端，或先执行： source ~/.bashrc
nvm install --lts
```

**验证：**

```bash
node -v
npm -v
```

> **摄像头权限（Linux 特有）**：确认设备存在并且你有读写权限
> ```bash
> ls -l /dev/video*
> ```
> 如果设备属主是 `root:video`，把自己加进 video 组，然后**重新登录**：
> ```bash
> sudo usermod -aG video $USER
> ```
> 浏览器在 `http://127.0.0.1` 下可以正常调用摄像头（localhost 被视为安全上下文），不需要额外配置。

---

### 不想装 Node.js？用 Python 也行

这个项目本质是**静态网页**，任何静态服务器都能跑。macOS 与大多数 Linux 发行版自带 Python 3：

```bash
cd motion-fitness-game
python3 -m http.server 4174 --bind 127.0.0.1
```

Windows 上如果装了 Python，把 `python3` 换成 `python`：

```powershell
cd motion-fitness-game
python -m http.server 4174 --bind 127.0.0.1
```

> ⚠️ 需要 **Python 3.9 或更高**：更老的版本不会给 `.wasm` 文件返回正确的 `application/wasm`，
> 浏览器会拒绝加载姿态模型。如果没有新版 Python，还是用 Node 更省事。

---

## 启动与访问

**用 Node（推荐）：**

```bash
cd motion-fitness-game
node preview-server.js
```

看到这两行就说明起来了（脚本会中英文各打印一行）：

```
体感健身游戏预览地址： http://127.0.0.1:4174
Motion Fitness is running at: http://127.0.0.1:4174
```

**用 Python：** 见上一节，同样是访问 4174 端口。

然后在浏览器打开：

### 👉 <http://127.0.0.1:4174>

首次进入点「开启摄像头」→ 浏览器弹窗选「允许」→ 选一个动作 → **进入画面里的虚线人体轮廓**，
全身识别一完成（虚线轮廓消失）就自动 3-2-1 倒计时并开始计数。

> ⚠️ **不要直接双击 `index.html`**。用 `file://` 打开时浏览器会拦截摄像头与 wasm 加载，
> 必须通过 `http://127.0.0.1:...` 访问（localhost 被浏览器视为安全上下文）。

**换端口 / 让别人也能访问**

```bash
# Windows PowerShell
$env:PORT=8080; node preview-server.js

# macOS / Linux
PORT=8080 node preview-server.js
```

默认只监听 `127.0.0.1`（仅本机可访问）——这既是隐私保护，也避免被同一网络里的其他人连上。

---

## 摄像头权限设置

### Windows

1. **设置 → 隐私和安全性 → 摄像头**
   - 打开「**摄像头访问**」
   - 打开「**让应用访问你的摄像头**」
   - 往下找到「**让桌面应用访问你的摄像头**」，确认是开的
2. 在浏览器里，点地址栏左侧的 🔒 / 摄像头图标 → 把「摄像头」改成 **允许**，然后刷新页面
3. 首次运行 `node` 时 Windows 防火墙可能弹提示：因为只监听 127.0.0.1，**允许或取消都不影响使用**

### macOS

1. 首次点「开启摄像头」时，浏览器会弹出系统授权窗口 → 点「**允许**」
2. 如果之前误点了「不允许」：**系统设置 → 隐私与安全性 → 摄像头** → 勾选你用的浏览器（Chrome / Safari / Edge），然后**完全退出浏览器再重新打开**
3. Safari 用户注意：Safari 需要在「设置 → 网站 → 摄像头」里允许当前站点

### Linux

1. 确认 `/dev/video*` 存在且有权限（见上面「Linux 安装 Node.js」一节）
2. 浏览器点地址栏左侧图标 → 允许摄像头
3. 如果用的是 Flatpak / Snap 版浏览器，可能需要在系统设置或 `flatpak override --device=all` 里放行摄像头设备

---

## 运动前校准

选好动作后，画面里会出现一个**虚线的人体剪影**：一条干净的外部轮廓线（不是骨骼连线），这就是你的姿势目标；
画面上方还会有一行文字提示，直接告诉你「进入虚线轮廓内」。校准面板会逐项打勾：**只有「识别到人体」和「全身入镜」两项是必须的**（✓ 达标、○ 没达标就会拦着开始）；
其余五项用「·」标出，只是**建议**——照着调整会让识别更准，但不影响开始。

| 检查项 | 含义 |
|---|---|
| 识别到人体（**必须**） | 摄像头看清你了 |
| 全身入镜（**必须**） | 头顶到脚都在画面里，没被画面边缘切掉 |
| 距离合适（建议） | 身体在画面里的大小合适（太远/太近会给方向提示） |
| 左右居中（建议·仅站姿） | 身体落在轮廓中间（躺姿不检查这一项） |
| 高度合适（建议） | 身体在画面的上下位置合适 |
| 机位正确（建议） | 正面动作（自重深蹲 / 相扑深蹲 / 深蹲跳 / 开合跳 / 跳箱 / 波比跳）要**正对**镜头，其余要**侧对**镜头 |
| 保持不动（建议） | 站定不动识别更稳（不站定也能开始） |

**每个动作都有自己的剪影**（按动作的机位与体态自动选用），摆好姿势照着轮廓对就行：
正面动作（自重深蹲 / 相扑深蹲 / 深蹲跳 / 开合跳 / 跳箱 / 波比跳）是正面站姿剪影，站姿类动作是侧面站姿，
俯卧撑类（含登山者）是**侧面的俯卧撑起状态**（直臂撑地），平板支撑与侧平板是**侧面小臂撑地的俯卧状态**，
臀桥 / 卷腹 / 仰卧抬腿 / 死虫式这些躺姿动作是**侧面的仰卧屈腿状态**（仰卧、屈膝、双脚踩地）。
侧拍时轮廓会跟着你的朝向自动左右翻转。

**必须的两项达标**并保持约 0.6 秒后，**虚线轮廓会立刻消失**（这就是「已识别到你的全身」的信号），
随后自动 3-2-1 倒计时开始计数，不需要再点任何按钮。
一组结束时会回到校准：这一轮轮廓会停在绿色，由你点「开始训练」（或按空格）开始下一组 ——
这样可以先休息、看看小结，不会被立刻拽进下一组。

> 站不到位时，画面上方的文字提示和校准面板里都会直接写出该做什么（例如「往镜头方向挪一点」「往右挪一点」「请正对摄像头」），
> 不会出现"不知道哪里不对"的情况。
> 站姿动作（自重深蹲 / 箭步蹲 / 波比跳这类）与躺姿一样**容忍出画约 3%**：前后脚拉开、脚尖贴着边缘都不会被拦，
> 只有身体明显有一边在画面外才提示「往后退半步或往里挪一点（不用站到正中间）」。
> 躺着的动作（俯卧撑 / 平板支撑 / 臀桥 / 卷腹这类）同样只要求「身体大致在画面里」，而且**允许出画约 3%**：
> 躺着时身体横跨整幅画面，头顶或脚碰到边缘完全不影响识别；躺姿也**不再要求「左右居中 / 高度合适」**
> —— 横躺的身体本来就不会正对画面中央。距离按**身体长度**判断，不用站起来。
> 想重新校准：点画面下方的「重新校准」按钮；每一组结束后也会自动回到校准。

---

## 动作主页与动作页

**主页**：进站先看到分类动作墙，按 **上肢 / 下肢 / 核心 / 全身 / 拉伸** 分成五块，
每块里每个动作一张卡片：图标、名称、计数还是计时、默认目标、以及**判定依据**（这个动作靠什么判定）。
上方搜索框可以按名称直接找（比如输入「俯卧撑」或「push」）。

- 点任意卡片 → 进入**动作页**（摄像头 + 要领清单 + 计分 + 目标设置 + 记录）。
  动作页在地址栏里是独立的地址：`#/ex/<动作 id>`（例如 `#/ex/bridge`），所以**刷新会停在同一个动作**、
  **浏览器后退回到主页**，也可以直接把链接存成书签。
- 动作页左上角的 🏠 图标（悬停/读屏名称是「返回主页」，快捷键 `H`）回到动作墙；右上角是 🎯 **运动设定**（只管这一个动作）和 ⚙️ **设置**（全局）——
  三个都是图标按钮，顶栏不再有文字按钮，小屏幕上也不挤。
- **🎯 运动设定**（只在动作页出现，和 ⚙️ 并排）：弹窗里集中这个动作的目标与判据 ——
  本组目标（数字框 + 预设快捷值）、这个动作的**判定依据**、**机位提示**，
  以及下面这组 **📐 关键帧判据与判分**（逐格列出关键帧的判据与分数）。
  侧栏的「② 设定目标」只剩一行目标摘要，点旁边的按钮同样能打开它。
- 动作可以同时属于多个分类（`cats` 里写几个就出现在几块，点进去是同一个动作）；现在 22 个动作各自只属一个分类。

**画面左上角的两个大数字**：**次数**（计时类动作是**秒数**）在最上面，**分数就紧贴在它下面** ——
用户要求「分数要放在和次数靠近的地方，比如在次数下方，而且要字体要用不同的颜色」，
所以分数是**金色（`--score`）**、和白色的次数、青色的目标进度环都不一样，一眼就能分清哪个是分。
次数和分数**共用一套跳动反馈**：计上一次、要领加分、每秒 +1 分时两个数一起跳一下。
右下角那个圆环只显示**本组目标进度（%）**。

## 🟩 判定进度条（画面上那条一格一格点亮的线条图标链）

动作页画面上**一直显示**一条判定进度条（宽度铺满视频底边的约 95%，图标高约 60~80px）：这个动作的关键帧被摊成几格，每格画一个**火柴人线条图标**，
表示「到这一步身体应该是什么姿势」。每识别到一个姿态就点亮一格，一眼就能看到「已经认出来了什么」，
**每完成一次动作就清零**，下一轮从头再走一遍。

以箭步蹲为例（格子里就是代码里的真实阈值）：

```
① 站直      ② 迈步      ③ 蹲下      ④ 后膝下沉   ⑤ 回位（计次）
≥145°      ≤146°      ≤152°      ≤158°      站起来
 人形        人形        人形        人形        人形
 ✓          ✓          ✓          ✓          ⏳
```

> **链上每一格都是「计一次数必须满足的条件」，最后一格就是计次那一刻** ——
> 所以「所有关键帧都做完了」= 这一次已经计上了，不会出现「都做对了却没计次」。
> 不影响计次的判据（深度 / 满分、时间类）**不进进度条**，它们作为补充判据列在「📐 关键帧判据与判分」弹窗里对应的那一格下面，
> 分数照旧按深度给（`tests/test-detectors.mjs` 会逐动作验证「链走完」与「计次」相差 ≤250ms）。

- **两种状态**：**没识别到人**时整条是灰的（尚未开始）；**识别到人并且在计数**时变成彩色（已经开始工作）。
  「做到 / 没做到」有**四重区别**，一眼就能分出来：没做到 = 虚线灰框 + 又细又淡的灰线条；已做到 = 实心亮绿底 + 加粗亮绿线条 + 右上角**大号绿色对勾徽标**（48px 圆盘、34px 勾、3px 描边、外发光）+ 整格发光；正在等的那一格 = 琥珀色粗描边 + 轻轻呼吸。
  识别本身会抖，所以短暂丢帧（0.7 秒以内）不会闪成灰色，也不会清掉进度。
- **格子上只画线条图标**：每个图标都是按这一格的姿态画的火柴人 ——
  「前膝屈角 ≤ 152°」就画一条膝角 152° 的腿；「髋部抬起高度 ≥ 0.22×躯干长」就把臀桥画到那么高；
  「离地高度 ≥ 0.035×画面高」就画成跳起来；俯卧撑这种撑地动作则让**手固定在地面上、身体高度由肘角决定**
  （肘越弯身体越低）。为了让 146° 与 152° 这种细微差别也看得出来，画的时候把弯曲幅度放大了一点，
  但**顺序与判据严格一致**（判据越难画得越极端）。
- **图标本来就能区分就不写数字；实在像就标出度数**：同一个动作里有两格用同一个关节角、而两个度数相差
  ≤12°（画出来几乎一样）时，这条链上所有该关节角的格子都会在图标里**标出判据的度数**——
  俯卧撑就是典型：`俯撑 → 146° → 138° → 144°`（否则 146° 和 144° 画出来几乎一模一样）。
  深蹲这种本来就一眼能区分的链（176°→135°→112°→149°）不标数字，画面上不会全是数字。
- **臀桥的三个关键帧**（用户指定的顺序）：`屈腿仰卧 → 曲腿腰臀顶起 → 恢复屈腿仰卧`。
  第 1 格和第 3 格姿势本来就一样（都是屈腿仰卧），所以**第 2 格加向上箭头、第 3 格加向下箭头**，
  一眼就能看出「这一格是往下回到地面」。**计次发生在「落回地面」那一刻** —— 也就是第 3 格点亮的同一刻。
- **鼠标移到某一格上 → 进度条上方会显示这一格的判定标准**（例如「蹲下 · 前膝屈角 ≤ 152°」），
  顺便带上这一格能拿多少分，松开鼠标就收起来；平时画面上只有图标和分数，不占地方。
- **一格是怎么亮的**：只看「下一格」，所以必须按顺序一格一格走，不会跳格；已经亮的格不会退回去。
  格的判定线优先用**识别器自己的动态线**（深蹲的 standLine、俯卧撑的 backLine、箭步蹲的 exitLine、
  臀桥的 topLine / atBottom、通用屈伸类的 progress 比例线），所以「界面说到线了」和「识别器判到位了」永远是同一条线。
- **最后一格一定看得见**：通用屈伸类的「回位」用的是识别器自己的比例线（`progress ≤ 0.16`），
  图标则按弹窗里那条真实判据画（例如「膝屈角 ≥ 157°」，画成腿还差一点没伸直），
  刻意与第一格「站直（176°）」画得不一样 —— 否则「画得一模一样就合并」的规则会把它吃掉，
  变成「进度条满了、却没计次」（这个坑真的踩过，`tests/test-specs.mjs` 现在逐动作守着）。
- **一次动作完成 → 整条链点亮并保持 0.65 秒，然后清零重来**：识别器计上一次的那一刻，
  进度条会把整条链补满（哪怕个别格的判据刚好卡在临界值上），让你明确看到「这一轮走完了」；
  0.65 秒后自动清零，下一轮从第一格重新走。半程动作（没计上次数的那种）同样走完 + 清零。
- **分数就写在关键帧里（字体做大）**：每个动作的分都**分配到具体的关键帧**上（分配表见 `src/specs.js`
  的 `STEP_STAGE`）—— 深蹲 = 站姿 4 / 开始 6 / 计次 21 / 回位 8 + 整轮满分 6；
  臀桥 = 仰卧 5 / 顶起 20 / 落回 8 + 满轮 6；俯卧撑 = 俯撑 5 / 开始 7 / 计次 14 / 回位 8 + 满轮 6。
  还没拿到时，格子上用**灰色小字**标出「这一格可得多少分」；**真达标拿到分**时，换成**大号亮黄色数字**
  （并垫一层深色底、闪一下），所以「动作做得标准不标准」在关键帧上直接看得见。
  计时类的每秒加分也记在最后一格上，会一直往上加。
- **配套反馈**：亮一格会轻响一声（逐级升高，最后一格补一个高八度）；加分时那一格闪一下，
  同时画面上方飘一下「+N」。分数只在识别器真正给分的那一刻出现，不会因为「姿势看起来对了」就提前跳分。
- **进度条不会抖**：格子是**一次性建好的**，之后每一帧只改「该改的那一格」的 class 和数字。
  以前每次加分/点亮都重建整条 HTML：元素被换掉，颜色过渡、呼吸、对勾和分数弹出动画都会**从头再放一遍**，
  一秒一次（计时类每秒加分）就成了肉眼可见的抖动。现在状态没变时**一次 DOM 写入都不会发生**
  （`tests/test-app.mjs` 用「class 实际变化次数 = 0」和「元素始终是同一批」两条断言守着），
  上方「本帧 +N」牌子的高度也固定住，出现时不会把整条进度条顶下去。
- **各动作走几格**（画出来一模一样的格子会自动合并，避免「假两步」）：
  深蹲 4 格（站直 → 半蹲 → 深蹲 → 回位）；箭步蹲 5 格（站直 → 迈步 → 蹲下 → 后膝下沉 → 回位）；
  俯卧撑 4 格（俯撑 → 肘 ≤146° → 计次 ≤138° 或肩膀下沉 0.14 → 回到顶位）；
  跳跃类多一格「起跳」（整幅图离地）；波比跳 4 格（站直 → 下蹲 → 俯撑 → 起跳）；
  臀桥 3 格（屈腿仰卧 → 腰臀顶起 → 落回屈腿仰卧，**落回那一刻计次**）；
  平板支撑/侧平板按「撑起来 → 离地 → 手贴地」走（计时类没有计次，链条以姿势到位收尾）；
  仰卧类（卷腹 / 抬腿）按躯干卷起或腿抬起的幅度走，最后回到起始位计次。
- **数值来源同一处**：图标与进度条用的阈值、「📐 关键帧判据与判分」弹窗、识别器判定**完全是同一批常量**
  （见 `src/specs.js` 的 `specStages` 与 `src/icons.js`），不会出现「进度条说到了、但没计上」的不一致。
  `tests/test-specs.mjs` 会逐格核对图标里的角度、检查图标不越界、并用合成骨架走一遍动作，
  验证进度条确实一格一格前进、浅动作不会走到最后一格。

## 🖐 一组结束后的手势圆环（退出 / 再做一次）

一组做完（达标自动收尾，或自己点「结束本组」）之后，画面上会浮出**两个大圆环**：
**左边「退出」**（回到动作主页）、**右边「再做一次」**（计数、分数、要领清单全部清零，马上重新开始这一组）。
刚练完手上都是汗、离键盘也远，这两个最常用的选择不该逼你去点屏幕。

- **所有动作都一样**：计数的（深蹲、俯卧撑、臀桥…）和计时的（平板支撑、侧平板、两种体前屈）做完一组都会摆出这两个圆环，
  自动达标收尾和自己点「结束本组」两条路也一样 —— 不存在「某些动作做完没得选」。
- **怎么用**：把**任意一只手掌**（左右手都行）放进圆环**中央**，保持 **3 秒**——
  圆环会**从 12 点开始顺时针**一圈一圈填满并转成绿色，填满那一刻就触发；手提前拿开，进度会退回去。
- **3 秒是刻意选的**：抬手、路过、犹豫一下都不会误触；圆环中央的判定区大约是环半径的 60%，
  擦到边缘也不算。
- **鼠标/触屏点一下圆环同样有效**（手感一样：点下去也会响一声），键盘还有 `Esc`（结束本组）和 `R`（重置计数）两条老路。
- **画面镜像时手掌位置跟着翻**：镜像预览下你看到的画面是左右翻的，圆环没有翻，所以判定会跟着镜像设置走，不会出现「手伸向左边却触发了右边」。
- **圆环不会被「人不在轮廓里」收掉**：卧姿类动作（臀桥 / 卷腹 / 平板 / 坐姿体前屈）做完时人本来就不在站立轮廓里，
  这时候圆环照样留着、不会一闪就没。**只有真的要开始下一组**（自动开始或点开始）、换动作、回主页时才收起。
- **圆环摆出来时画面上方只留一条提示**（「把手掌放进圆环中央保持 3 秒」）：校准提示条会临时让位，两条提示不抢位置。
- **一次只会触发一个**：两个圆环各自计时，谁先停满 3 秒就触发谁（一只手依次停留也不会误触另一个）。

## 📐 关键帧判据与判分（运动设定弹窗里）

每个动作的「运动设定」弹窗只列一组 —— **🎬 关键帧与判分**：把画面上那条进度条的**每一格**逐格列出来，
格子里的**线条图标**、序号 + 短标签、**这一格的判定标准**（就是进度条格子上那条判据）、
以及**这一格能拿多少分**；最后一格上还会打一个绿色徽标标出**「计次那一刻」**
（计时类动作是**「开始计时」**）。底下那行小结会写「共 N 格 …… 每格分数加起来 + 整轮满分 = 每轮 X 分」。

**判据完全挂在关键帧上**：弹窗里已经没有单独的「计次判据（做到什么程度算一次）」与「姿势要求」两组，
只有关键帧 + 下面的「姿态提醒」（只出声纠正、不吃次数的那几条）。与某一格绑定的补充判据会写在这一格下面：

- **第一格**（进入动作的姿势）：其余姿势要求（躯干、离地高度…）；
- **「计次」那一格**：深度到位线、以及「更浅只算晃了一下」的那条线；
- **最后一格**（计次那一刻 / 开始计时）：一轮最短用时、计时类的起算时间与容错宽限期。

以深蹲为例（数值就是代码里的常量，和画面上的格子一一对应）：

| 关键帧 | 判定标准 | 判分 |
|---|---|---|
| ① 站姿 | 髋比膝高 ≥ 0.86×小腿长 | +4 |
| ② 开始 | 髋比膝高 ≤ 0.78×小腿长 | +6 |
| ③ 计次 | 髋比膝高 ≤ 0.62×小腿长 | +21（下沉 7 + 蹲到水平 14） |
| ④ 回位 · **计次那一刻** | 髋比膝高 ≥ 0.86×小腿长 | +8 + 整轮满分 6 |

以箭步蹲为例（分组只剩关键帧 + 姿态提醒）：

| 关键帧 / 分组 | 判定标准 | 判分 |
|---|---|---|
| ① 站姿 | 双腿伸直角 ≥ 145° | +4 |
| ② 开始 | 前膝屈角 ≤ 146° | +11 |
| ③ 计次 | 前膝屈角 ≤ 152° | +11 |
| ④ 双腿 | 较直那条腿的膝角 ≤ 158° | +14 |
| ⑤ 回位 · **计次那一刻** | 从本轮最弯处回升 60%，或比最弯处高 8° 以上 | +8 + 整轮满分 6 |
| 姿态提醒 | 后膝接近地面（只出声，不吃次数） | 后膝离地高度 ≤ 0.66×小腿长 |

> 每一格下面还会带上它自己的补充判据，例如深蹲第一格写「还要满足：请正对摄像头、躯干倾角 ≤ 20°」，
> 计次格写「深度到位（拿满深度分）：髋比膝高 ≤ 0.4×小腿长 · 比这还浅只算晃了一下：> 0.74×小腿长」，
> 最后一格写「一轮最短：≥ 0.48 秒」；箭步蹲的第一格写「请侧对摄像头、躯干倾角 ≤ 32°」，
> 姿态提醒里还有「膝盖不要内扣」。

> 关键帧那一组和画面上的进度条**用的是同一份数据**（`app.js` 的 `criteriaModel()`：
> 同一批格子、同一批图标、同一份「得分分配到关键帧」的表 `stagePoints()`），
> 所以弹窗里写的「第几格要什么、拿多少分」和你在画面上看到的永远一致，
> `tests/test-app.mjs` 会逐条核对格数、图标、分数与总分。

> **这些数字不是另写的一份说明，而是从识别器真正使用的常量里取出来的**（见 `src/specs.js`）：
> 手写识别器读 `SQUAT / LUNGE / PUSHUP / BRIDGE / PLANK` 的阈值表，通用引擎读该动作在
> `catalog.js` 里的 `up / down` 与 `enter / bottom / loose / ignore` 进度，姿势门控读
> `engines.js` 的 `GATE_LIMITS`（同一张表也用于判定），计时类读 `HOLD_PRIME_MS / HOLD_GRACE_MS`。
> 改了阈值，弹窗里的数字自动跟着变，不会出现「界面说的和实际判的不一样」。
> `tests/test-specs.mjs` 会在每次测试时把这些数字**喂回识别器本体**验证：
> 弹窗里那条计次线必须正好落在识别器自己的进度线上（整个套件 2043 项断言）。

## 设置弹窗（语言 / 模型 / 声音）

动作页右上角 ⚙️ 打开设置弹窗，里面集中了全部开关：

| 分组 | 项目 |
|---|---|
| 语言 | 中文 / English（立即生效，动作名与要领一起切换） |
| 识别模型 | 轻量（流畅，默认）／完整（更准，首次切换需要多下载一份模型，之后离线可用） |
| 声音 | 🔊 语音报数 · 🎵 音效 · 🎶 背景音乐 |
| 画面与识别 | 🪞 镜像 · 🦴 火柴人 · 📐 角度 · 🐞 识别指标 |

点弹窗外部或按 `Esc`、`G` 都能关闭。

> **角度标签不会镜像**：开启 🪞 镜像预览时，视频与画布都会被左右翻转，画在画布上的
> 「膝 132°」「肘 118°」这类标注也会跟着反着写。渲染器会把自己再翻一次抵消掉，
> 所以镜像开着时关节角度读数依然是正常可读的。

---

## 怎么用（机位很重要）

**正面机位：自重深蹲、相扑深蹲、深蹲跳、开合跳、跳箱、波比跳；其余动作都是侧对镜头。**
动作页的「动作要领」第一行会写明这个动作该怎么站（每张卡片上也标了判定依据）。

- **正面动作**（自重深蹲 / 相扑深蹲 / 深蹲跳 / 开合跳 / 跳箱 / 波比跳）：正对摄像头。深度用「髋比膝高多少」判断，
  这个量在正视图里不被压缩；而且正对镜头才能看清膝盖有没有内扣、左右是否对称。

- 与摄像头相距 **2~3 米**，让**全身入镜**（头顶到脚都在画面里）；
- **箭步蹲 / 向后箭步蹲**：侧对镜头站立，画面里能同时看到脚踝、膝盖、髋、肩；
- **俯卧撑 / 平板支撑 / 登山者**：人趴的方向与镜头垂直，手和脚都在画面内；
- **臀桥 / 卷腹 / 仰卧抬腿 / 死虫式 / 侧平板**：躺姿，侧对镜头，能同时看到肩、髋、膝、踝；
- **拉伸**：站立体前屈侧对镜头站直；坐姿体前屈坐在地上侧对镜头；
- 光线均匀、背景别太乱，穿贴身一点的衣服识别更稳。
- **没有声音？** ① 看标签页有没有被静音（标签上的小喇叭图标）② 打开设置点一次「🔊 语音报数」，会立刻试播一句 ③ 打开「🐞 识别指标」，最后一行的「声音」应显示 running 且语音包数大于 0。

**注意流程是：选动作 → 进入虚线轮廓 → 全身识别完成（虚线轮廓消失）→ 自动 3-2-1 倒计时 → 开始计数；
一组结束后回到校准，再点「开始训练」（或按空格）开始下一组。**
校准阶段只做就位判断，不会计数也不会得分。

**快捷键**：`1`~`9` 快速切动作 · `H` 主页 · `G` 设置 · `空格` 开始/暂停 · `R` 重置计数 · `Esc` 结束本组 · `M` 镜像 · `S` 火柴人 · `F` 视频框全屏

---

## 动作库总览（22 个动作）

| 分类 | 动作（图标） | 类型 | 判定依据 | 默认目标 |
|---|---|---|---|---|
| 💪 上肢 | 标准俯卧撑 💪 | 计数 | 肘关节屈伸 | 12 次 |
| 💪 上肢 | 宽距俯卧撑 ↔️ | 计数 | 肘关节屈伸 | 12 次 |
| 💪 上肢 | 窄距俯卧撑 💎 | 计数 | 肘关节屈伸 | 10 次 |
| 🦵 下肢 | 自重深蹲 🏋️ | 计数 | 膝关节屈伸（正面深度） | 15 次 |
| 🦵 下肢 | 相扑深蹲 🤼 | 计数 | 膝关节屈伸 | 15 次 |
| 🦵 下肢 | 向前箭步蹲 🚶 | 计数 | 前膝角度 + 后膝高度 | 16 次 |
| 🦵 下肢 | 向后箭步蹲 ↩️ | 计数 | 膝关节屈伸 | 16 次 |
| 🦵 下肢 | 臀桥 🌉 | 计数 | 髋部抬起高度 | 15 次 |
| 🦵 下肢 | 深蹲跳 🚀 | 计数 | 屈膝 + 双脚离地 | 12 次 |
| 🔥 核心 | 平板支撑 🧘 | 计时 | 姿势保持时间 | 45 秒 |
| 🔥 核心 | 侧平板支撑 🧎 | 计时 | 身体姿势是否到位（粗略判定） | 30 秒 |
| 🔥 核心 | 死虫式 🐞 | 计数 | 左右腿交替 | 16 次 |
| 🔥 核心 | 卷腹 🌀 | 计数 | 肩离地高度 | 20 次 |
| 🔥 核心 | 反向卷腹 🔃 | 计数 | 髋关节折叠 | 15 次 |
| 🔥 核心 | 仰卧抬腿 🦿 | 计数 | 腰腿夹角（腿抬起的高度：躺平 180° → 抬到垂直 90°） | 15 次 |
| 🤸 全身 | 波比跳 💥 | 计数 | 整套动作顺序（蹲→撑→跳） | 10 次 |
| 🤸 全身 | 登山者 ⛰️ | 计数 | 左右腿交替 | 24 次 |
| 🤸 全身 | 开合跳 🙌 | 计数 | 双腿开合幅度 | 50 次 |
| 🤸 全身 | 跳箱 🦘 | 计数 | 屈膝 + 双脚离地（粗略判定） | 10 次 |
| 🤸 全身 | 弓步跳 ⤴️ | 计数 | 屈膝 + 双脚离地 | 14 次 |
| 🧘 拉伸 | 站立体前屈 🙇 | 计时 | 身体姿势是否到位 | 30 秒 |
| 🧘 拉伸 | 坐姿体前屈 🧎‍♂️ | 计时 | 身体姿势是否到位 | 30 秒 |

> 「深蹲跳」只归在**下肢**（用户要求从全身移出）；全身里的「开合跳」是新增的，默认目标 **50 次**。
> 一个动作可以同时属于多个分类 —— `cats` 数组里写几个分类就出现在几块（现在 22 个动作各自只属一个分类）。
> 判定依据是**摄像头真正测量到的东西**；标了**粗略判定**的动作（侧平板、跳箱等）只能判断「身体姿势大致到位」，
> 分数与计时照常，但不要把它当成严格的姿态裁判。

---

## 计分规则

界面右侧有一张**要领清单**，做对一步就立刻打勾、加分、响铃，当前该做哪一步会高亮。
**整轮要领全部完成**还有额外的满分奖励；计时类动作**每保持 1 秒再加 1 分**。

### 深蹲（每轮满分 45 分）

深蹲用**正面**机位，深度用「髋比膝高多少 / 小腿长」判断（站直 ≈ 1.0，蹲到大腿水平 ≈ 0）：

| 要领步骤 | 判定条件 | 分值 |
|---|---|---|
| ① 正对摄像头站好，全身入镜、身体直立 | 正面视角 + 全身可见 + 身体竖直 + 髋明显高于膝（比值 >0.86） | +4 |
| ② 屈膝下蹲，髋部向下沉 | 髋膝高度差比值 ≤0.72（大腿离水平约 46°） | +6 |
| ③ 双膝顺着脚尖方向屈曲下沉 | 比值 ≤0.50（约 30°） | +7 |
| ④ **蹲到大腿接近水平** | 比值 ≤0.25（大腿离水平约 15° 以内，或更低） | **+14** |
| ⑤ 蹬地站直，髋膝完全伸展 | 比值回到 ≥0.80 | +8 |
| 🎁 整轮要领全过 | 上面 5 步同一轮内全部达成 | +6 |

### 箭步蹲（每轮满分 54 分）

| 要领步骤 | 判定条件 | 分值 |
|---|---|---|
| ① 侧对摄像头站好，身体直立、全身入镜 | 侧拍视角 + 全身可见 + 身体直立 + 两腿基本伸直 | +4 |
| ② 一条腿向前迈出，前后脚分开 | 两脚踝前后距离 > 0.45 倍躯干长 | +5 |
| ③ 迈成一大步（前后距离拉开） | 两脚踝前后距离 > 0.9 倍躯干长 | +6 |
| ④ 双膝同时弯曲下沉，前膝约 90° | 更弯的一条腿 ≤118° 且另一条 ≤150° | +11 |
| ⑤ **后膝下降到接近地面** | 后膝离地高度 ≤0.35 倍小腿长 | **+14** |
| ⑥ 前脚蹬地回到站姿 | 两腿重新伸直（需先下沉过） | +8 |
| 🎁 整轮要领全过 | 上面 6 步同一轮内全部达成 | +6 |

### 俯卧撑（每轮满分 40 分）

| 要领步骤 | 判定条件 | 分值 |
|---|---|---|
| ① 双手撑地，身体成一条直线 | 趴撑姿 + 身体直线角 ≥150° + 偏差 ≤0.18 | +5 |
| ② 收紧核心，肘部弯曲下沉 | 肘角 ≤138° | +7 |
| ③ **肘角弯到胸口接近地面** | 肘角 ≤118° 且身体仍成直线 | **+14** |
| ④ 推起还原，手臂接近伸直 | 肘角 ≥142°（需先下放过） | +8 |
| 🎁 整轮要领全过 | 上面 4 步同一轮内全部达成 | +6 |

> **「回到起始位」的判定线跟着你自己走（重要）**：真机上机位与投影会让「手臂伸直 / 站直」的读数偏低
> （侧拍俯卧撑常常只有 140~150°），如果按固定角度要求「必须回到 145/168°」才算一轮结束，这一轮就永远不结算，
> 后面每次都被并进同一轮 —— **做十个只记一两个**。现在起始位用「你最近 1.5 秒实际到过的最大幅度」当基准，
> 回到自己顶位附近就算完成；万一卡住，超过 9~12 秒也会强制结算，绝不吞次数。
> 另外：所有计数类动作都在 🐞 面板里给了「计数诊断」行，可以直接看判定线和上次没计上的原因。

> **箭步蹲不要求前膝 90°**：前膝弯到 152°（从站直弯下去约 20°）就算一次，`enter`/`exit` 两条线都跟着
> **你自己站得最直时能到多少度**自适应 —— 读数被压缩（站直只有 140°）也不会乱计数，深蹲得深的人也不会被吞次数。
> 「太快」只对真的沉下去过的那些轮次做检查，浅的一次不会因为做得快被判半程。

> **箭步蹲要求两条腿都弯（不能只点前腿）**：只看前膝的话，前腿点一下就能凑一次。现在还会盯住
> **较直的那条腿（后腿）**：它也要弯到 **158°** 以内（或比你自己站直时弯下去 **12°** 以上，取更严的一条），
> 才算一次有效次数；只有前腿动就记为半程，并语音提示「两条腿都要弯下去：后腿也要屈膝下沉」。
> 后腿弯得少一些但确实弯了照样计次 —— 放宽的大前提没变。🐞 面板的「双腿最弯(后膝/线)」会显示这一轮的实测值与判定线。

> **俯卧撑的计数放宽了（大体做了就算一次）**：肘角弯到 **138°** 以内、再回到 **146°** 以上就算一次
> （原来是「必须沉到 124°、还要手臂全直回到 152°」；满分深度也从 124° 放宽到 **128°**，
> 要领步骤里的「弯到胸口接近地面」同步从 105° 放宽到 **118°**）。塌腰/撅臀/身体不够直只会**语音纠正 + 质量分打折**，
> 不再吃次数；沉得不够也会照常计数，但会提示「再低一点」、分数按深度递减。
> 只有「几乎没弯」（肘角没低于 146°）才不计次也不出声。
> **进度条链上「计次」那一格就是计次那一刻**：肘到 138°（或肩膀下沉到位）点亮它，最后一格「回到顶位」再点亮，
> 整条链走完 —— 也就是「四个关键帧全做完」和「计上一次」是同一件事（`tests/test-detectors.mjs` 逐动作核对过）。
> 没有严格模式：这一格也就是唯一的计次判据。

> **摄像头看不到贴地也能计次（相机角度补偿）**：笔记本摆在桌上斜着往下拍时，画面里根本看不到胸口贴地，
> 而且 2D 投影会把肘角**读得比真实更直**（最弯也可能只有 140°）。所以俯卧撑多了一路与肘角无关的深度证据：
> **肩膀离地高度下沉了多少**（撑起时约 1.2 倍躯干长，压到底只剩 0.5 左右）。肩膀沉下去 **0.14 倍躯干长**
> 就算「身体确实接近地面」并计次，沉到 **0.30** 倍给满分（这两个数原本是 0.20 / 0.40，一起放宽了）；
> 连「这一轮开始了」也用肩膀下沉 **0.08** 倍来起头。
> 两路证据任意一路成立就认，所以机位高低都不用再迁就。🐞 面板的「肩下沉量」会实时显示这个数。

### 臀桥（每轮满分 39 分）

| 要领步骤 | 判定条件 | 分值 |
|---|---|---|
| ① 仰卧屈膝，双脚与髋同宽踩实 | 仰卧屈膝姿（肩贴地、膝离地） | +5 |
| ② 收紧臀部，把髋部向上顶起 | 髋抬起量 > 0.15 倍躯干长 | +6 |
| ③ **顶到肩-髋-膝接近一条直线** | 髋抬起量 > 0.35 倍躯干长 | **+14** |
| ④ 臀部有控制地落回地面 | 用自己的「落回地面」判定线（需先顶起过） | +8 |
| 🎁 整轮要领全过 | 上面 4 步同一轮内全部达成 | +6 |

> **臀桥的计次线跟着你自己走，而且「三个关键帧全做完」就一定计次**：原来要求髋部抬起量 > 0.35 才计入深度
> 且「落回 0.12 以下」才算一轮结束 —— 但每个人躺平时髋部的基准高度并不一样，基准偏高的人**一躺下就已经超过 0.12**，
> 于是顶起来那一次永远结算不了，**后面全部并进同一轮，做多少都只记一次**（就是「三个关键帧都做对了却没计次」）。
> 现在改成：识别器自己记「你这一组的最低点」当基准，`落回地面 = 回到自己最低点往上 0.08 以内`，
> `顶起线 = 自己最低点往上 0.16（且不低于 0.22）`；**计次发生在「从顶点落回地面」那一刻** ——
> 也就是进度条第 3 格「恢复屈腿仰卧」点亮的同一刻，整条 3 格链走完并计一次。
> 一整轮（离开地面 → 落回地面）少于 0.42 秒只当抖动，不计数、只提示放慢一点。
> 🐞 面板的「髋抬起量(基准)」会同时显示实测值和当前判定线。

### 平板支撑（一次 70 分 + 每秒 1 分）

| 要领步骤 | 判定条件 | 分值 |
|---|---|---|
| ① 小臂在肩下撑地，把身体撑起来 | 肩离地 + 手贴地 + 肘角合法 | +8 |
| ② 头、背、髋、脚踝成一条直线 | 身体直线角 ≥148° + 不塌腰不撅臀 | +12 |
| ③ 稳定保持 3 秒 | 保持满 3 秒 | +10 |
| ④ 稳定保持 10 秒 | 保持满 10 秒 | +15 |
| ⑤ 稳定保持 30 秒 | 保持满 30 秒 | +25 |
| ⏱ 每保持 1 秒 | 姿势有效期间 | +1/秒 |

> **平板支撑的判定放宽了**：只要「身体基本放平 + 肩离地 + 手/小臂在地面附近」就开始计时，
> 塌腰、撅臀、身体不够直、膝偏低这些都只是**语音纠正 + 质量分打折**，不再中断计时；
> 只有「根本没撑起来」（站着、趴着不动）才不计时。其余计时类动作同理：宽限期 1.2 秒内的小幅晃动不打断。

### 关于不在清单里的动作

> 这一版动作库按给定的清单精简为 22 个动作（静态臀桥、悬垂举腿、俄罗斯转体等都不在其中）—— 需要它们的话，
> 在 `src/catalog.js` 里复制一条 `bridge` 的配置、把 `kind` 改成 `'hold'`，再跑一次 `npm test` 就能加回来
> （识别引擎与计分方案都是现成的，见[想加动作](#想加动作--调分值--改阈值)）。

### 通用「族」计分方案

除了上面 5 个手写方案，其余动作共用**族方案**（同一类的动作判定逻辑一致，只是阈值不同）：

| 族方案 | 用在哪类动作 | 步骤构成 |
|---|---|---|
| 站姿屈伸 | 相扑深蹲、向后箭步蹲 | 站好 → 屈膝下沉 → 做到目标幅度 → 蹬地还原 |
| 俯撑屈伸 | 宽距/窄距俯卧撑 | 撑好成一条线 → 屈肘下沉 → 到目标深度 → 推起还原 |
| 仰卧抬起 | 卷腹、反向卷腹、仰卧抬腿 | 躺好 → 启动发力 → 抬到位 → 控制着放回 |
| 左右交替 | 死虫式、登山者 | 摆好姿势 → 第一次收/伸 → 换另一侧 → 保持节奏 |
| 多段动作 | 波比跳 | 站好 → 下蹲撑地 → 完成中段 → 起身收尾 |
| 跳跃类 | 深蹲跳、弓步跳、跳箱 | 站好 → 屈膝蓄力 → **双脚离地** → 屈膝落地 |
| 开合跳 | 开合跳 | 双脚并拢站好 → 打开、双手上举 → 开到最大 → 收回并拢 |
| 计时（姿势） | 侧平板支撑 | 摆好姿势 → 身体成一条线 → 坚持 3 / 10 / 30 秒 |
| 计时（拉伸） | 站立体前屈、坐姿体前屈 | 进入拉伸姿势 → 放松呼吸 → 保持 10 / 20 秒 |

计时类族方案同样是**每保持 1 秒 +1 分**，姿势垮掉超过 1.2 秒会暂停计时并语音提醒。

> **仰卧抬腿怎么判**：判据就是**髋关节的角度**（腿和上身的夹角，单位是度）——
> **躺平、腿伸直贴地 ≈ 180°** → 腿绷直抬起来、角度一路变小 → **抬到腿垂直地面、与上身约 90°** → 放下来回到 ≈ 180°，如此循环。
> `up = 180` 是起始线（跟着你自己躺平的角度自校准：驼背躺不平的人按他自己的最大角度算），`down = 90` 是抬到垂直。
> 进度条四格 = **躺平 → 开始抬起（≤151°）→ 计次（≤105°）→ 放回躺平（≥158°，带一个向下箭头）**。
> 阈值按用户要求都留了宽容，两头都不严格：
> - **抬到髋关节约 90° 就计次**：计数线放在 **105°**（离垂直 15° 以内都算一次），满分深度线 **95°**（基本垂直）；
> - **腿放平就能开始下一次**：回位线放在 **158°** —— 离躺平还有 22° 就算回到起始位，不会因为没完全放平而不结算；
> - 起始格画的是**躺平 + 腿伸直**（不是臀桥/卷腹那种屈膝姿势）；如果膝盖弯着抬（夹角一样能凑到 90°），
>   会语音提醒「腿要绷直」—— **只提醒、不扣次数**。宽容线是**膝盖角度 ≥ 130°**（用户要求「不要太严格，130° 以上都可以接受」）：
>   只有弯过 130° 才出声，弹窗的「姿态提醒」里也列着这条线。
> - 画面上的角度标注写的是**「髋 xx°」**（判据那个数字）—— 和别的动作**同一套叫法**，全应用只有「髋」这一种写法
>   （不再有「腰部角度」「胯」这类特例）。
> - **姿势要求也放宽到最少**（用户要求）：进入判定的姿势要求**只剩「肩离地高度 ≤ 0.6×躯干长」一条**，
>   原来那条「躯干倾角 ≥ 36°」已经删掉 —— 半躺着（躯干斜得不够）也算仰卧，照样进入判定；
>   站姿仍旧进不来（站立时肩离地高度在 1×躯干长以上，这一条本身就把它挡住了）。
>   死虫式共用旧的 `supineLow` 门控，仍旧是两条都看。

> **开合跳怎么判**：正对镜头，用**双膝横向距离**（`kneeSpread`，单位是躯干长）量「开合」——
> 并拢站好约 0.35、跳开站宽约 1.5，进度 = (最窄 − 当前) / (最窄 − 最宽)，所以「并拢 → 打开 → 并拢」正好是一轮。
> 判定线跟着**你自己这一组最窄的站距**走（引擎的 `effUp` 自校准），所以本来就站得开的人也照样计次。
> 速度快是正常的（默认最短一轮 0.3 秒），只把「上下抖一下」挡掉。关键帧上的图标是**正面火柴人**：
> 并拢（手臂放下）→ 打开（双腿张开、双手举过头顶）→ 开到最大 → 收回并拢（带一个向下的箭头，表示回到起始位）。

### 音效与语音反馈

| 时机 | 反馈 |
|---|---|
| 达成一个要领 | **音阶上行的一声铃** + 画面飘出 `+12` 加分动画 |
| 整轮要领全过 | 三音上行「满分」和弦 + 加分飘字 |
| 每个要领第一次达成 | **语音念出该要领**，之后重复只响铃不念，避免刷屏 |
| 每跨过 50 分 | 上扬提示音 + 语音报分 + 字幕祝贺 |
| 有效次数 +1 | 五声音阶计次音 + 语音报数 |
| 动作没算数 | 低频「噗」声 + 字幕指出问题 |
| 计时类保持中 | 每秒一声轻点 + 分数持续上涨 |
| **计时类每满 5 秒** | **语音读秒「5 秒」「10 秒」…（平板支撑 / 侧平板 / 体前屈都一样）**，屏幕上的秒数同时跳一下 |
| 达标 / 结束本组 | 庆祝和弦 + 达标飘字 + 结算面板（含得分与漏掉的要领） |

音效与语音都可以在 ⚙️ 设置 弹窗里一键关闭。

> **计时类读秒**：平板支撑这类计时动作**每 5 秒播报一次当前秒数**（「5 秒」「10 秒」「15 秒」…），
> 用的是识别器自己累计的计时，所以：暂停（姿势垮掉）后继续接着数，不会从头念；换动作或重置计数后
> 从 5 秒重新开始；秒数一次跳过好几档（中途被打断很久）时只读当前这一档，不会连珠炮。
> 和「稳定保持 10 秒」这类要领语音撞在同一帧时，**读秒优先**（要领照样加分、打勾、响铃，只是不念那句话），
> 免得两句叠在一起听不清。语音关掉时不会出声，但画面上的秒数仍然会跳一下。

---

## 多语言

界面内置中文和英文两种语言，⚙️ 设置 里的 **语言** 下拉框随时切换，选择会被记住：

| 语言 | 代码 | 词条文件 |
|---|---|---|
| 中文 | `zh` | `src/locales/zh.js` |
| English | `en` | `src/locales/en.js` |

首次打开时会按浏览器语言自动选择（中文用中文，其余语言一律回退到英文）。

> 之前还做过西班牙文和法文，为了方便开发已经移除（只保留中英两种）。
> 想再加语言时按下面的步骤走就行，词条结构是现成的。

**新增一种语言**：复制 `src/locales/en.js` 改成新语言，在 `src/i18n.js` 里 import 并加进 `LOCALES` 与 `LANG_ORDER`，
再跑一次 `npm run test:i18n` —— 测试会逐条检查缺键、漏翻、占位符与数组长度，保证不会漏。
⚙️ 设置里的语言下拉框会自动多出一项；`tests/test-i18n.mjs` 里的 README 对比清单需要手工加一行。

---

## 站不进轮廓 / 没反应？排查四步

**① 先确认版本。** 页面标题旁应显示 `v3.0`。如果没看到，说明浏览器还在用缓存的旧版本 —— 按 **Ctrl + F5**（Mac 是 Cmd + Shift + R）强制刷新。

**② 先看右侧的「运动前校准」面板。** 哪一项没打勾，照着面板下方那句话做即可（**只有「识别到人体」和「全身入镜」两项会拦着开始**；躺姿动作的项目更少）：

| 面板提示 | 含义 |
|---|---|
| 没看清你的全身：请退后一点… | 模型没找到人或部位出画 |
| 离摄像头太远 / 太近：往前走一点 / 往后退一点 | 身体在画面里的大小不对 |
| 往右站一点 / 往左站一点，站到轮廓中间 | 左右没居中 |
| 往画面上方 / 下方站一点 | 上下位置不对 |
| 请正对摄像头 / 请侧对摄像头 | 机位与当前动作不匹配 |
| 很好，保持不动… | 只差最后一秒了 |

**③ 再看画面下方的状态条**，它会同步显示同一句话；开始训练后则显示「下一步该做什么、还差多少」。

**④ 打开 ⚙️ 设置 → 「🐞 识别指标」**（校准或训练中都可用）。画面下方会实时列出识别器看到的数字，例如：

```
视角 侧面✓(0.18) · 全身 ✓ · 双腿可见 ✓ · 躯干倾角 6° · 膝 176° · 肘 172° ·
髋 172° · 身体直线 175° · 髋抬起 -0.98 · 大腿离水平 88° · 可见度 0.94 · 状态 running
```

对照判断：

| 症状 | 原因 | 处理 |
|---|---|---|
| `视角 侧面✗(0.90)` | 机位和当前动作要求不一致（正面动作要正对、其余要侧对） | 按状态条提示转身：自重深蹲 / 相扑深蹲 / 深蹲跳 / 跳箱 / 波比跳正对镜头，其余侧对 |
| `全身 ✗` | 有人体部位出画 | 退后 1~2 步，让头顶到脚都进画面 |
| `没检测到人体` | 太远/太近、逆光、背景与衣服同色 | 靠近一点、面对光源、换衣服、换分辨率更高的摄像头 |
| `躯干倾角` 长期 > 32° | 摄像头是斜的或人站歪了 | 摆正摄像头（或用书本垫平） |
| 数字正常但没打勾 | 卡在某一步的阈值上 | 状态条会直接写出还差多少（如「现在已经蹲到 62%（100% = 大腿水平）」） |

---

## 常见问题

**Q：端口 4174 被占用怎么办？**
换端口启动即可：`PORT=8080 node preview-server.js`（Windows 用 `$env:PORT=8080; node preview-server.js`）。
查占用：macOS/Linux 用 `lsof -i :4174`；Windows 用 `netstat -ano | findstr 4174`。

**Q：浏览器说"相机不可用 / NotAllowedError"？**
按上面「摄像头权限设置」把系统和浏览器两层的权限都打开，然后**完全退出浏览器再重开**。

**Q：页面提示"姿态模型加载失败"？**
多半是 `.wasm` 的 MIME 不对。用自带的 `node preview-server.js` 最稳；用 Python 需要 3.9+。
另外确认 `vendor/` 目录完整（从 GitHub 下载 ZIP 时最容易漏解压整个目录）。

**Q：画面卡顿 / 帧率低？**
在 ⚙️ 设置 里把「模型」切到 **轻量**、关掉「📐 角度」；换一台性能更好的设备。识别在 10~15 FPS 下依然可用。

**Q：手机能用吗？**
可以。手机浏览器打开同一个地址即可（手机与电脑需在同一局域网，且服务器要监听 `0.0.0.0`：
把 `preview-server.js` 里的 `'127.0.0.1'` 改成 `'0.0.0.0'` 后重启）。注意这样同一网络里的其他人也能访问。

**Q：数据存在哪里？**
训练记录与最佳成绩存在浏览器的 localStorage 里，换浏览器或清缓存会丢，不会上传到任何服务器。

---

## 测试

```bash
npm test                       # 六套测试一起跑（1614 项）
npm run test:i18n              # 多语言：缺键 / 漏翻 / 占位符 / 数组长度 / 源码残留中文 / 中英两份 README 结构
npm run test:detectors         # 五个手写识别器的识别与计分逻辑（合成骨架驱动）
npm run test:engines           # 通用识别引擎（屈伸 / 左右交替 / 转体 / 多段动作 / 计时 + 姿势门控）
npm run test:specs             # 「关键帧判据与判分」：弹窗显示的数字必须等于识别器真正使用的判定线
npm run test:dump              # 额外打印基线姿势指标，便于调阈值
npm run test:page              # 页面接线自检（DOM id / 模块导入 / 静态资源 / 动作库与分类清单）
npm run test:app               # 最小 DOM 桩加载真实 app.js 的集成测试
```

| 测试文件 | 项数 | 覆盖内容 |
|---|---|---|
| `tests/test-i18n.mjs` | 18 | 中英两种语言键结构一致、无漏翻、占位符与数组长度一致、源码里无写死中文、中英两份文档结构一致 |
| `tests/test-detectors.mjs` | 284 | 五个手写识别器的标准/错误动作计数、计时、要领得分与顺序，斜机位下的深度判据，运动前校准的判定，以及「判定进度条走完」与「计上一次」的一致性（两者相差 ≤250ms） |
| `tests/test-engines.mjs` | 158 | 通用引擎：一次循环一次数、只有宽松一档（没有严格模式）、晃动与过快的边界、姿势门控（含仰卧抬腿放宽后的「只看肩离地高度」）、跳跃离地、左右交替、整套动作、计时暂停恢复 |
| `tests/test-specs.mjs` | 851 | 把弹窗里的数值喂回识别器：必须正好落在它自己的计次线上；姿势门控数值与判定用的同一张表（仰卧抬腿只剩「肩离地高度 ≤ 0.6×躯干长」一条，死虫式仍是两条）；22 个动作都有指标；计次链条每一格都是计次的必要条件（最后一格 = 计次那一刻，深度/时间类判据不进条）；通用屈伸类的最后一格就是引擎自己的回位比例线（不会是恒真的假格子）；关键帧线条图标的角度与判据一致、且「计次那一刻」那一格不会被合并掉；两种语言文案齐全 |
| `tests/test-page.mjs` | 359 | DOM 接线、模块导入导出、静态资源、22 个动作的分类清单与计分方案完整性，以及画面上「分数紧贴次数、用专属金色」的样式断言 |
| `tests/test-app.mjs` | 373 | 真实 `app.js` 的启动、主页渲染、运动设定（含关键帧判据与判分）与设置两个弹窗、判定进度条（灰/彩状态、逐格点亮、悬停显示判据、一圈走完清零）、校准流程、切动作、计分、音效、结算、中英切换，以及「分数就排在次数下方」的版面断言 |

---

## 目录结构

```
motion-fitness-game/
├─ index.html            页面结构（文案走 data-i18n）
├─ style.css             深色 UI 样式
├─ preview-server.js     零依赖本地服务器（http://127.0.0.1:4174）
├─ src/
│  ├─ i18n.js            ★ 多语言内核（t / setLang / applyI18n）
│  ├─ locales/           ★ zh.js / en.js 两份词条（中英）
│  ├─ catalog.js         ★ 动作库：五大分类 + 22 个动作（图标、类型、引擎、阈值、判定依据）
│  ├─ geometry.js        几何与信号处理（角度、One Euro 平滑）
│  ├─ metrics.js         每帧动作指标（关节角、髋抬起量、离地高度、身体直线度…）
│  ├─ steps.js           ★ 每个动作的「要领计分步骤」（判定条件 + 分值 + 提示键）
│  ├─ calibration.js     ★ 运动前校准：虚线人体剪影轮廓 + 七项就位判定
│  ├─ detector-base.js   识别器基类（要领计分、提示节流、丢帧处理）
│  ├─ engines.js         ★ 通用识别引擎（屈伸 / 左右交替 / 转体 / 多段动作 / 计时 + 姿势门控）
│  ├─ exercises.js       ★ 五个手写识别器（自重深蹲 / 向前箭步蹲 / 俯卧撑 / 臀桥 / 平板支撑）+ 工厂
│  ├─ pose-engine.js     MediaPipe PoseLandmarker 封装 + 摄像头管理
│  ├─ render.js          骨架绘制
│  ├─ audio.js           音效 + 语音播报（跟随语言）
│  └─ app.js             界面、动作主页、设置弹窗、训练流程、要领清单、记录、主循环
├─ tests/                六套自动化测试
└─ vendor/               MediaPipe tasks-vision（wasm）与姿态模型（离线）
```

---

## 想加动作 / 调分值 / 改阈值

**加一个动作只要在动作库里加一条配置**（不用写识别代码）：

1. 在 `src/catalog.js` 的 `EXERCISES` 里加一条，例如
   `e('myMove', '🔧', ['core'], { plan: 'repSupine', posture: 'supine', judge: 'clear', target: 15, params: bend({ metric: 'kneeClear', gate: 'supineLow', up: 0.15, down: 0.85 }) })`；
2. 在两份 `src/locales/*.js` 的 `ex` 里补上 `myMove: { name, cameraHint, goal }`
   （要领与注意事项会自动套用所属「族」的模板，想单独写就加 `howto` / `tips`）；
3. 跑 `npm test` —— 测试会检查五类清单、中英两种语言的文案、每个动作的识别器与计分方案是否齐全。

- **改分值或要领文案**：编辑 `src/steps.js`（结构与分值）与 `src/locales/*.js`（文案）。
  每个步骤就是一个 `{ id, labelKey, points, check, hint }`；`check(frame, det)` 返回 `true` 即表示这一步达成。
- **改判定阈值**：
  - **通用引擎动作**（库里绝大多数）：改 `src/catalog.js` 里该动作的 `params` ——
    `metric` 用哪个量（`kneeBent` / `elbow` / `hipRise` / `kneeClear` / `shoulderClear` …）、
    `gate` 要求什么姿势（`stand` / `prone` / `supine` / `seated` / `sideLying` …）、
    `up` 起始值、`down` 到位值、`looseP` 宽松计数线、`minRepMs` 最快用时、`flight` 是否要求离地；
  - **五个手写识别器**：改 `src/exercises.js` 里对应常量（都有中文注释）——
    `SQUAT_FRONT`（在 `src/steps.js`）：`standRatio` 0.86 / `enterRatio` 0.78 / `bottomRatio` 0.40 /
    `looseRatio` 0.62，单位是「髋比膝高多少 ÷ 小腿长」，站直 ≈ 1.0；
    `LUNGE`：`enterKnee` 146 / `looseKnee` 142 / `downKnee` 128 三档膝角，`backKneeDrop` 0.66，
    `enterHoldMs` / `exitHoldMs` 是抖动宽容时间；`PUSHUP.elbowFull` 106 / `looseElbow` 124；
    `BRIDGE.upRise` 0.22、`minRepMs` 700；`PLANK.bodyStraight` 142；
  - `HoldDetector.graceMs`：计时类动作的容错宽限期（默认 1200ms）。
  - **只有宽松一档**：`DetectorBase` 里已经没有 `strict` 开关了（大体做到就计次数，动作不标准只用语音纠正 +
    分数打折）；计次判据只有一条 —— 进度条上的关键帧链全部点亮。

改完请跑 `npm test`，测试里已经写好了这些动作的标准幅度，能立刻告诉你是不是调过头了。

**识别原理**：所有判据只用**角度**和**除以躯干长的比例**，与身高、机位距离无关；
关键点先按画面宽高比校正再算角度，并用 One Euro 滤波抑制抖动，所以同一套阈值适配不同的人和机位。

---

## 隐私与许可

- 画面只在本机浏览器里处理，**没有任何上传**；模型与 wasm 都是本地文件，断网可用。
- 服务器默认只监听 `127.0.0.1`，同一网络的其他人无法访问。
- 训练记录只存在你自己的浏览器 localStorage 里。
- 第三方资源：`vendor/` 下的 MediaPipe Tasks Vision（wasm）与 Pose Landmarker 模型来自 Google MediaPipe，遵循 **Apache License 2.0**。
